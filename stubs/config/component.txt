import { defineConfig } from 'vite';
import shared from './shared.js';

export default defineConfig({
  build: {
    emptyOutDir: false,
    ...shared,
    lib: {
      entry: 'src/js/[file].temp.ts',
      name: '[name]',
      fileName: 'js/components/[file]',
      formats: ['iife'],
    },
  },
  esbuild: {
    legalComments: 'none',
  },
});
